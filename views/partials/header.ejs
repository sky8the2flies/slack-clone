<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Klack - Simplified slack clone</title>
    <link rel="stylesheet" href="stylesheets/style">
</head>
<body class="container">
    <div class="nav-container">
        <div class="nav-side-container">
        </div>
        <div class="name-main-container">
                <% if (channel && channel.current) { %>
                    <h1>#<%= channel.current %></h1>
                <% } %>
                <% if (user) { %>
                    <h2>Welcome, <%= user.name %></h2> | <a href="/logout">Logout</a>
                <% } else { %>
                    <a href="/auth/google">Login with google</a>
                <% } %>
        </div>
    </div>
