<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://kit.fontawesome.com/c6d585037c.js" crossorigin="anonymous"></script>
    <title>Klack - Simplified slack clone</title>
</head>
<body class="container">
    <div class="nav-container">
        <div class="nav-side-container">
            <h1>KLACK</h1>
        </div>
        <div class="nav-main-container">
            <div class="nav-channel-name">
                <% if (channel && channel.current) { %>
                    <h1><i class="fab fa-slack-hash"></i><%= channel.current.name %></h1>
                    <p><%= channel.current.description %></p>
                <% } %>
            </div>
            <% if (channel.current) { %>
                <div class="nav-channel-options">
                    <% if (channel.current && channel.current.channelType === '1' && user && user._id.equals(channel.current.members[0])) { %>
                        <a href="/channels/<%= channel.current._id %>/members/new"><i class="fas fa-user-plus"></i></a>
                    <% } %>
                    <% if (channel.current && user && user._id.equals(channel.current.members[0])) { %>
                        <a href="/channels/<%= channel.current._id %>/edit"><i class="far fa-edit"></i></a>
                        <form action="/channels/<%= channel.current._id %>?_method=DELETE" method="POST">
                            <button><i class="delete fas fa-backspace"></i></button>
                        </form>
                    <% } %>
                </div>
            <% } %>
        </div>
        <div class="nav-user">
            <% if (user) { %>
                <h3>Welcome, <%= user.name %></h3>
                <a href="/logout">Logout <i class="fas fa-sign-out-alt"></i></a>
            <% } else { %>
                <a href="/auth/google">Login with <i class="fab fa-google"></i></a>
            <% } %>
        </div>
    </div>
